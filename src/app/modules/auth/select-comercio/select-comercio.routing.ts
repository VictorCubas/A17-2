import { Route } from '@angular/router';
import { SelectComercioComponent } from './select-comercio.component';

export const selectComerciodRoutes: Route[] = [
    {
        path     : '',
        component: SelectComercioComponent
    }
];
